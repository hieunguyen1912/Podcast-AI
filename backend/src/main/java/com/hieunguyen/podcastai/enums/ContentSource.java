package com.hieunguyen.podcastai.enums;

/**
 * Enum for content source types in audio generation
 */
public enum ContentSource {
    NEWS_API,      // From News API
    TEXT_INPUT,    // From user text input
    URL_INPUT,     // From URL (future)
    FILE_UPLOAD    // From file upload (future)
}
